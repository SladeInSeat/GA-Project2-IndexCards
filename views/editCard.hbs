<main>
    <div class="showTopicContainer">
        <div class="topicData">
            Prompt: {{card.cardPrompt}}<br>
            Answer: {{card.cardAnswer}}<br>
            Parent Topic Id: {{card.parentTopic}}<br>
            Card Id: {{card._id}}<br>
        </div>
        <div class="topicActions">
                <form action="/{{card.parentTopic}}/editCard/{{card._id}}?_method=PATCH" method="POST">
                    <input type="text" name="cardPrompt" id="cPromptInput" value={{card.cardPrompt}}>
                    <label for="cPromptInput">Card Prompt</label>
                    <input type="text" name="cardAnswer" id="cAnswerInput" value={{card.cardAnswer}}>
                    <label for="cAnsweInput">Card Answer</label>
                    <input type="submit" value="Submit changes">
                </form>
        </div>
    </div>
    <a href="/">Back to Landing Page</a>
    <br>
    <a href="/showUsers">Show Existing Users</a>
</main>      